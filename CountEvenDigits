#include <vector>
#include <cmath>
using namespace std;

int findNumbers(vector<int>& nums) {
    int count = 0;

    for(int n : nums) {
        int digits = (n == 0) ? 1 : floor(log10(n)) + 1;
        
        if(digits % 2 == 0) {
            count++;
        }
    }

    return count;
}
